#include <list>
#include <iostream>
#include <iterator>

using namespace std;

int main()
{
    int l,n,size,c=0;
    cin >> l; cin >> n;

    for (int i=0;i<l;i++)
    {
        cin >> size;
        list<int> m[i];
        for (c;c<size;c++)
        {
            m[i].pushback(c);
        }
    }

    list<int> path;
    for(list<int>::iterator i=m[0].begin)

}
